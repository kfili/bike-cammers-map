<br>
<form {{action 'save' on='submit'}}>
  {{input placeholder='channel name'
          maxlength="70"
          required='true'
          value=channel.title}}<br>
  {{input placeholder='link to channel'
          type='url'
          required='true'
          value=channel.url}}<br>
  {{input placeholder='city where bike cammer is based'
          maxlength="40"
          required='true'
          value=channel.city}}<br>
  {{input placeholder='country OR state, country'
          maxlength="40"
          required='true'
          value=channel.state}}<br>
  <button type='submit' class='btn-md btn-primary'>Save</button>
  <button {{action 'cancel'}} class='btn-md btn-default pull-right'>back</button>
</form>
